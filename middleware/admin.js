export default function({ store, redirect }) {
  //   const { auth } = store.state;
  //   const permissions = auth.payload.role;
  //   if (!auth.payload || !permissions || permissions >= 0) {
  //     return redirect("/not-permited");
  //   }
}
